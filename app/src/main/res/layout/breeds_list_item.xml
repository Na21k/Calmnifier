<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/page_margin"
    android:layout_marginVertical="@dimen/list_item_margin"
    android:clickable="true"
    app:cardCornerRadius="@dimen/corner_radius"
    app:cardElevation="@dimen/elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/breed_photo"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_margin="0dp"
            android:contentDescription="@string/breed_photo_content_description"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_image_24" />

        <TextView
            android:id="@+id/breed_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="@dimen/card_content_margin_vertical"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="@dimen/card_content_margin_horizontal"
            android:ellipsize="end"
            android:maxLines="1"
            android:textSize="@dimen/title_text"
            tools:text="Some breed name" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>